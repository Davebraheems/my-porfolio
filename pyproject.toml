[project]
name = "portfolio"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "flask==3.1.2",
    "gunicorn==21.2.0"
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
